# generated by @ng-toolkit/serverless
org: simonschiebler
app: blckbook
service: frontend

provider:
  name: aws
  runtime: nodejs14.x
  region: eu-west-1

plugins:
  - serverless-finch

custom:
  bucketName:
    dev: dev.blckbook.vote
    prod: blckbook.vote
  client:
    bucketName:  ${self:custom.bucketName.${opt:stage, self:provider.stage}}
    distributionFolder: dist/blckbook-ui
    indexDocument: index.html
    errorDocument: index.html
